version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    develop:
      watch:
        - path: ./package.json
          action: rebuild
        - pathL: ./next.config.js
          action: rebuild
        - path: ./package-lock.json
          action: rebuild
        - path: .
          target: /app
          action: sync
    environment:
      - DB_URL=mongodb+srv://michaeljaviermota:GiNn4b39nPbkps3b@cluster0.3ytnvqr.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

volumes:
  tasked:
